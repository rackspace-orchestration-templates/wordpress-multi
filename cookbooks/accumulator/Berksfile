site :opscode

metadata

group :integration do
  cookbook "accumulator_test",
    :path => "test/accumulator_test"
  cookbook "mail_alias",
    # :path => File.expand_path("~/git/chef-mail_alias")
    :git => git://github.com/kisoku/chef-mail_alias.git, :branch => 'develop'
end
